/**
 * Created by bridgelabz on 28/1/17.
 */

public class CommentedCode {
    //firebase
      /*  DownloadData downloadData = new DownloadData(getActivity());
        downloadData.getReference(new IData() {
            @Override
            public void requireData(ArrayList<EmployeeModelData> arrayInterfaceData) {

                mData = arrayInterfaceData;
                for (int i = 0; i < mData.size(); i++) {
                    Log.i("bye....in attendence", mData.get(i).getEmployee_company());
                }
                EmployeeAdapter adapter = new EmployeeAdapter(getActivity(), mData);
                mRecyclerView.setAdapter(adapter);
            }
        });*/
  /*  DownloadData downloadData = new DownloadData(getActivity());
        downloadData.getReference(new IData() {
            @Override
            public void requireData(ArrayList<EmployeeModelData> arrayInterfaceData) {

                mData = arrayInterfaceData;
                for (int i = 0; i < mData.size(); i++) {
                    Log.i("bye....in attendence", mData.get(i).getEmployee_company());
                }
                EmployeeAdapter adapter = new EmployeeAdapter(getActivity(), mData);
                mRecyclerView.setAdapter(adapter);
            }
        });*/
}
